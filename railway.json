{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "php artisan storage:link --force && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT",
    "healthcheckPath": "/up",
    "restartPolicyType": "ON_FAILURE"
  }
}